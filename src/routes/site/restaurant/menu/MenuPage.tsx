/**
 * MenuPage — `/site/restaurant/menu`. Placeholder until the Menu
 * Management module ships.
 */

import { ZoneSplash } from "../../../../components/Layout/ZoneSplash";

export function MenuPage() {
  return (
    <ZoneSplash
      zone="restaurant"
      title="Menu"
      subtitle="Categories, items, ingredients, modifiers, and pricing."
    />
  );
}
