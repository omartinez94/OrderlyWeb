/**
 * ReservationsPage — `/site/restaurant/reservations`. Placeholder
 * until the Reservations module ships.
 */

import { ZoneSplash } from "../../../../components/Layout/ZoneSplash";

export function ReservationsPage() {
  return (
    <ZoneSplash
      zone="restaurant"
      title="Reservations"
      subtitle="Time × service × table grid. Click empty cell to book."
    />
  );
}
