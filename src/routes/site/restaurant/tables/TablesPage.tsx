/**
 * TablesPage — `/site/restaurant/tables`. Placeholder until the
 * Tables & Floor Plan module ships.
 */

import { ZoneSplash } from "../../../../components/Layout/ZoneSplash";

export function TablesPage() {
  return (
    <ZoneSplash
      zone="restaurant"
      title="Tables"
      subtitle="Live floor view and floor-plan editor. Sections, table states, drag-to-rearrange."
    />
  );
}
