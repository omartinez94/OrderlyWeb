/**
 * OrderDetailPage — `/site/restaurant/orders/:id`. Placeholder
 * until the order detail module ships.
 */

import { ZoneSplash } from "../../../../components/Layout/ZoneSplash";

export function OrderDetailPage() {
  return (
    <ZoneSplash
      zone="restaurant"
      title="Order detail"
      subtitle="Items, modifications, activity log, and status transitions."
    />
  );
}
