/**
 * StaffNewPage — `/site/admin/staff/new`. Placeholder until
 * the Staff Management module ships.
 */

import { ZoneSplash } from "../../../../components/Layout/ZoneSplash";

export function StaffNewPage() {
  return (
    <ZoneSplash
      zone="admin"
      title="Invite staff member"
      subtitle="Send an invitation with role and restaurant assignment."
    />
  );
}
